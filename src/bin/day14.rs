use advent2024::{advent_main, multidim::{Point, Position}};
use bare_metal_modulo::*;

fn main() -> anyhow::Result<()> {
    advent_main(|filename, part, _| {
        
        Ok(())
    })
}

#[derive(Copy, Clone)]
struct Robot {
    p: Position,
    
}

impl Robot {
    fn new(line: &str) -> Self {

    }
}